[package]
name = "repopact-provider-github"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
repopact-remote-provider.workspace = true
serde.workspace = true
serde_json.workspace = true
reqwest.workspace = true

[dev-dependencies]
# WI067 Checkpoint C: the live end-to-end evidence test proves a real
# GitHub snapshot flows through WI065's actual production workspace
# pipeline, not a stand-in.
repopact-mobile-acquisition.workspace = true
tempfile = "3.23.0"
